# إغلاق ما بعد النشر — حادثة SHA1 + النسخ الاحتياطي للإنتاج (2026-07-24)

هذا التقرير يكمل `FINAL_CLOSEOUT_20260723_AR.md`: ما جرى بعد نشر الإغلاق (`9b64046`)، وكيف عولج، وما الدليل.

## 1) الحادثة الإنتاجية: `FUNCTION railway.SHA1 does not exist`

- **العرض:** بعد نشر الإغلاق، ثلاث شاشات إنتاجية تعطّلت بخطأ 500: قائمة أصناف المنيو (`/api/menu/list`)، كتالوج الكاشير (`/api/pos/v2/catalog`)، وقائمة صور المنتجات (`/api/product-images`).
- **السبب الجذري (مُثبت على قاعدة الإنتاج مباشرة):** MySQL الإنتاج **9.6.0** أزال الدالتين المدمجتين `SHA1()`/`MD5()`، بينما MySQL المحلي/البوابة **8.4.9** ما زال يملكهما — فالاستعلام يمرّ بكل الاختبارات محليًا ثم يرمي في الإنتاج. المواقع الثلاثة كانت تحسب رمز كسر كاش للصور: `SUBSTRING(SHA1(image_data),1,8)`.
- **الإصلاح (`f494468`، منشور وحيّ):** `SHA1(x)` ← `SHA2(x,256)` في `routes/menu.js` و`routes/pos-v2.js` و`routes/product-images.js`. دوال Node `crypto` (ETag/التدقيق) لم تُمسّ — لا تمر عبر MySQL.
- **الوقاية الدائمة:**
  - حارس ثابت جديد `scripts/audit/sql-removed-functions.js` — **أول خطوة في البوابة** — يُفشل أي SQL خلفي يستدعي دالة أزالها MySQL 9 (يمسح 225 ملفًا خلفيًا).
  - اختبارا القيمة الحرفية للرمز (`test:item-image`, `test:product-images-upload`) أُدخلا في البوابة (لم يكونا ضمنها، ولذلك تسرّب الانحدار).

## 2) النسخة الاحتياطية الكاملة للإنتاج — الحل والدليل

- **العائق:** `mysqldump` عبر بروكسي Railway العام (`maglev.proxy.rlwy.net:57552`) يتوقف دائمًا عند ~5MB (كامل/مضغوط/سكيما-فقط سواء) — قيد بروكسي على البث الطويل، لا قيد بيانات.
- **الحل:** أداة جديدة `scripts/backup/prod-dump.js` — نسخ منطقي باستعلامات قصيرة مجزّأة (1000 صف/استعلام) داخل `START TRANSACTION WITH CONSISTENT SNAPSHOT` واحد، مع:
  - استثناء الأعمدة المولّدة (VIRTUAL/STORED GENERATED) من الإدراج — بدونها تفشل الاستعادة بـ ER 3105 على `shift_close_denominations.total` (اكتُشف بالتجربة وأُصلح)؛
  - إزالة `DEFINER` من الـ views؛ ملف `manifest.json` يقارن عدّ الصفوف الحيّ بالمكتوب لكل جدول.
- **الدليل (نُفّذ فعليًا 2026-07-24):**
  - النسخ: **213 جدولًا + 2 view، لقطة متسقة (`consistent: true`)، صفر فروق عدّ، 4.72MB** — الملف: `Downloads\mt-pos-backups\prod-railway-20260724.sql`.
  - **إثبات الاستعادة:** استُعيدت النسخة كاملة في قاعدة محلية مؤقتة: **215/215 كائنًا، exit 0**، عيّنات مطابقة (menu = 146، audit_logs = 3332، shift_close_denominations = 25)، والنص العربي سليم (`آيسد أمريكانو`…). ثم حُذفت القاعدة المؤقتة.

## 3) تحقق الإنتاج بعد الإصلاح

- `/api/version` = `f494468` على `main`، الفروع الأربعة `/`، `/app`، `/pos-v2`, `/pos` تخدم HTML 200.
- على قاعدة الإنتاج 9.6 مباشرة: استعلام الكتالوج المُصلح (CASE + `SHA2`) يُنفَّذ على 144 صنفًا نشطًا بلا خطأ، و`warehouses.name_en` موجود (ترحيلات الإقلاع طُبقت).
- الشاشات المصادَقة تُفحص بصريًا من المالك؛ أي خطأ يُبلَّغ ويُعالج فورًا (البروتوكول المتفق عليه).

## 4) حالة البوابة ×3

- **الدورة 1 (قبل النشر، عند `9b64046`):** خضراء كاملة 19/19 خطوة.
- **الدورتان 2 و3 (عند `f494468`، ببوابة موسّعة 22 خطوة تشمل حارس SHA1 واختباري الصور):** أُطلقتا متسلسلتين وتعملان وقت كتابة هذا التقرير؛ النتيجة النهائية تُبلَّغ فور اكتمالهما. هذا الرفع (توثيق + سكربت تشغيل) لا يمسّ كود التطبيق، فشهادة الدورتين على كود الإنتاج تبقى صالحة.

## 5) المتابعات المفتوحة (موثقة، غير منفذة)

1. **مطابقة إصدار MySQL في البوابة:** تشغيل البوابة على MySQL 9.x محليًا (يتطلب تنزيل خادم 9.x ~250MB — بانتظار موافقة). إلى حينها: الحارس الثابت + الإثبات المباشر على إنتاج 9.6 هما خط الدفاع.
2. **تثبيت/فحص `@@version` و`sql_mode` عند الاتصال** في `db/connection.js` (يضبط charset+timezone فقط حاليًا) — تغيير كود يمرّ بالبوابة الكاملة، لذا أُجّل عمدًا عن مسار الإصلاح العاجل.
3. جدولة النسخ الاحتياطي دوريًا (الأداة الآن موجودة وقابلة للجدولة).
