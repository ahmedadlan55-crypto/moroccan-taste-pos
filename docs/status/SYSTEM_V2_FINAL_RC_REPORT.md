# SYSTEM V2 — Final Integrated RC + Staging UAT

> فرع `codex/system-v2-final-rc` — توحيد نهائي لـ Warehouse V2 + Cashier V2 داخل النظام الرئيسي الواحد.
> **لم يُدمج شيء إلى main ولم يُنشر شيء على Production.** Staging فقط. التاريخ: 2026-07-04.

---

## 1) Ancestry — توحيد الفرعين

```
git merge-base --is-ancestor 59992bf a4d44fa   → نجح
```
فرع الكاشير `a4d44fa` **يحتوي المستودعات `59992bf` كاملة** (سلفٌ مثبت). لذلك بُني الـ RC من `a4d44fa` مباشرةً، ثم دُمج آخر `main` (`dcc21653`) بفارق محتوى **صفر**:

- `git diff a4d44fa..<merge> = فارغ` (لا انحراف، لا تعارضات، لا إسقاط ميزات).
- `git merge-base --is-ancestor 59992bf HEAD` ✅ · `a4d44fa HEAD` ✅ · `dcc21653 HEAD` ✅.
- لا `artifacts/` ولا `dist/` ولا `node_modules/` متعقَّبة (تحقّق `git ls-files`). لا تعديلات مستخدم غير مرتبطة.

## 2) بوابة Clean HEAD (worktree نظيف + `npm ci`)

| الفئة | النتيجة |
|---|---|
| Backend unit (27 ملفًا) | ✅ |
| Integration suites (29 حزمة) | ✅ كلها خضراء |
| — production-orders API | ✅ 73/73 |
| — cashier V2 API | ✅ 41/41 |
| — inventory-tx / reverse / stocktake / lots / warehouses / scope / reports / … | ✅ |
| Navigation coverage E2E (متصفح حقيقي) | ✅ 123/123 |
| Cashier V2 E2E (يوم كاشير كامل) | ✅ 20/20 |
| Warehouse vitest / tsc / build | ✅ 102 اختبار / 0 خطأ / بناء ناجح |
| POS vitest / tsc / build | ✅ 33/33 / 0 خطأ / بناء ناجح |
| `node --check` لكل backend JS | ✅ |
| `npm audit` (root + المستودعات + الكاشير) | ✅ 0 ثغرات |
| Bootstrap من قاعدة فارغة | ✅ |
| Preflight | ✅ BLOCKER=0 |
| Zero placeholders (مسح المصدر) | ✅ |

**الإجمالي: 44/44 بندًا.** (سُدّت فجوة تنظيف في `purchaseReceipt` كانت تترك حركات دفعات يتيمة وتسبب BLOCKER زائفًا على قاعدة التطوير.)

## 3) نشر Staging (الخدمة الموجودة فقط)

- المشروع: `moroccan-pos-staging` · الخدمة: `warehouse-staging` · القاعدة: `MySQL` (staging) — **لا خدمة/قاعدة جديدة**، `prod-monitor` لم يُمس.
- الأعلام (Staging فقط): `WAREHOUSE_V2_ENABLED=1`, `POS_V2_ENABLED=1`, `WAREHOUSE_SCOPE_ENFORCE=1`.
- migrations قبل الاستماع · `/api/inventory/v2/ready` = **200** · timezone **+180** (Asia/Riyadh) · preflight **BLOCKER=0**.
- المسارات: `/warehouse` (200)، `/pos-v2` (200)، `/warehouse-v2/... → 301 /warehouse`، `/pos` القديم (200)، `/` الشل (200).
- الرابط: https://warehouse-staging-production-aa06.up.railway.app

## 4) UAT المتكامل (متصفح حقيقي ضد Staging)

نتيجة `scripts/uat-staging-rc.cjs` — **97/97 بندًا ✅ (0 إخفاق)** + **49 لقطة شاشة** (`artifacts/rc-staging-uat/`). أبرز ما أُثبت:

**التكامل داخل الشل (P1):** تسجيل دخول واحد → `/warehouse` داخليًا بلا login/شل ثانٍ، رابط مستودعات **واحد** في القائمة، زر «العودة للنظام الرئيسي»، رجوع/تقدم المتصفح، `/warehouse-v2 → 301`.

**مسح الصفحات (P2):** 22 صفحة/معالج تُرسم بلا أخطاء console، **صفر Placeholder/404**، **أرقام إنجليزية فقط** (لا ٠-٩) عبر كل الصفحات.

**عمليات المستودع (P3–P11):** إنشاء/تعديل مستودعات (UI)؛ صنف + باركود + بحث عالمي بالباركود؛ استلامات بدفعات FEFO؛ **تمرين Rollback عند فشل GL** (إسقاط `gl_entries` مؤقتًا → الترحيل يفشل بلا أي كتابة مخزون/حركة/دفعة، ثم ينجح مرة واحدة بعد الاستعادة)؛ استلام مشتريات **جزئي** (15 ثم 25 من 40)؛ سياسة السالب؛ تحويل بين مستودعين مع **FEFO**؛ جرد كامل بتسوية مرتبطة؛ **أمر إنتاج كامل** (معالج UI → اعتماد → صرف FEFO → إنتاج جزئي+هدر → إكمال 27+3=30 → إغلاق، رصيد FG=27)؛ تقارير + تصدير CSV (BOM).

**يوم كاشير كامل (P12) على `/pos-v2`:** جلسة واحدة؛ فتح وردية؛ **مسح باركود يضيف الصنف** (معرّف مشترك مستودع↔كاشير)؛ نقدي بباقٍ + بطاقة + **مختلط**؛ تعليق/تذكرة مطبخ/استعادة؛ **أوفلاين → مزامنة «مرة واحدة بالضبط»** (خصم مخزون −2 بالضبط)؛ رفض خصم الكاشير 15% سيرفريًا؛ إلغاء بسبب؛ إلغاء بعد الإكمال عبر الإشعار الدائن؛ إغلاق وردية بفروقات حية؛ صفر أخطاء console؛ أرقام إنجليزية؛ 375px.

**إثبات الربط مستودع↔كاشير (الأهم):** بيع نقدي لصنف متتبع خصم من **أقرب دفعة (FEFO) L1** ونقص رصيد المستودع بمقدار البيع مع **حركة دفعة مرجعها البيع** وقيد GL — كل ذلك عبر **مسار المال القديم الموحد**.

**عدم الازدواج (P12):** إعادة إرسال نفس `clientOrderId` ⇒ **نفس البيع بلا صف جديد ولا خصم مخزون/دفعة ثانٍ**.

**Rollback عند فشل المخزون (P12):** صرف V2 أكبر من الرصيد ⇒ **INSUFFICIENT_STOCK** مع **صفر تغيير** في المخزون/الدفعات/الحركات (ذرّية).

**Invariants نهائية (P13):** `Σ(أرصدة الدفعات) = رصيد المستودع` لكل صنف متتبع (انحراف=0)؛ **كل قيود GL متوازنة** (مدين=دائن) على مستوى القاعدة؛ عدّادات السلامة `lot_invariant_violation`/`gl_imbalance` = **صفر**.

**التعايش (P14):** الكاشير القديم `/pos` وAPIs القديمة تعمل بجانب الجديد؛ لوحة المستودعات تعمل بعد يوم كامل من الحركة.

## 5) الاستضافة الواحدة (مثبت من الكود)

- **Repository واحد** · **Express واحد** (`server.js` يركّب `/warehouse`, `/pos-v2`, `/api/pos/v2`, `/api/inventory/v2/*` بجانب كل مسارات النظام القديم) · **Database واحدة** · **Auth/JWT واحد** (`pos_token` مشترك — تنقّل حر بلا login ثانٍ) · **App Shell واحد** · **Docker build واحد** (مرحلة تبني SPA المستودعات ثم الكاشير في نفس الصورة).
- مستقبلًا: **مشروع Railway إنتاجي واحد** — لا خدمة إنتاج مستقلة للمستودع أو الكاشير. صفر تكلفة استضافة إضافية دائمة.

## 6) الأمان — تدوير كلمة admin

- الأداة التفاعلية `scripts/rotate-admin-password.js` (إدخال مخفي، bcrypt فقط، تحقق ذاتي: الجديدة تنجح والقديمة تفشل، سجل تدقيق بلا سر) جاهزة.
- الغلاف `scripts/maintenance/rotate-admin-prod.cjs` يعرض **هوية بيئة الإنتاج فقط** (المشروع/البيئة/الخدمة) بلا أي Secret، يطلب تأكيد `PRODUCTION`، ثم يشغّل الأداة على قاعدة الإنتاج عبر `DATABASE_URL` دون طباعة/حفظ السر.
- **التدوير الفعلي على الإنتاج بيد المالك** (يتطلب TTY وإدخال الكلمة الجديدة يدويًا). لا يغيّر JWT_SECRET ولا أي Feature Flag. تفاصيل بيئة الإنتاج المعروضة للتأكيد:
  - Railway project: **kind-quietude** · Environment: **production** · Service: **moroccan-taste-pos** (قاعدة: **MySQL**).

## 7) المخاطر المتبقية

- **JWT_SECRET الإنتاج < 32 حرفًا** → تحذير إقلاع؛ التدوير مؤجَّل حتى انتهاء مراقبة الـ24h (يبطل الجلسات). Runbook: `docs/guides/jwt-rotation-runbook.md`.
- **`POS_V2_ENABLED` على الإنتاج = OFF** حتى قرار المالك؛ حتى حينها `/pos` القديم هو المسار (rollback فوري بإطفاء العلم).
- **مودفَيرز/كومبو صريحة** غير موجودة في النظام أصلًا (تُحاكى بالوصفات) — خارج نطاق المطابقة الحالية.
- **ازدواجية الكتالوج القديمة** (أصناف INV-* خام ليست في `inv_items` catalog) — ملاحظة تشغيلية، بلا أثر وظيفي على المتتبع.
- الأصناف المُباعة مباشرةً في الكاشير يجب أن تكون `production_method='imported'` لتُرحّل مخزونها (نمط النظام القديم نفسه) — موثّق.

## 8) القرار

# ✅ SYSTEM V2 READY FOR PRODUCTION

جاهز للإنتاج تقنيًا — **بانتظار أمر المالك الصريح** بالدمج والنشر. لم يُدمج إلى main، ولم يُنشر على Production، ولم يتغيّر Canary/Scope/monitor. Staging باقٍ (خطة إيقافه في `STAGING_RESOURCES_PLAN.md`).
